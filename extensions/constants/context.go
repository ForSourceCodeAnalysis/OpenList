package constants

// context key
const (
	//StorageKey represents the context key for storage driver
	StorageKey = "storage"
	//MetaKey represents the context key for src dir
	SrcDirKey = "src_dir"
)
